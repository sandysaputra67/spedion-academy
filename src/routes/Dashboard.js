import React, { StrictMode } from 'react';
import { Students } from '../pages';
export const Dashboard = (props) => {
  return (
    <> <StrictMode>
    		
    		<Students/>

    	</StrictMode>
    </>
  )
}